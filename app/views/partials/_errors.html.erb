<% if o.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(o.errors.count, "error") %> prohibited
    this model from being saved:</h2>
  <ul>
  <% o.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
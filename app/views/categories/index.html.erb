<%= render 'partials/title', title: 'All Categories' %>

<ul>
<% @categories.each do |category| %>
	<li><%= link_to category.name, category_path(category) %> (<%= link_to 'edit', edit_category_path(category) %>)</li>
<% end %>
</ul>

<%= link_to 'New Category', new_category_path %>
<%= form_for @category do |f| %>

  <%= render 'partials/errors', o: @category %>
  
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.submit %>
  </div>

<% end %>